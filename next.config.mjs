/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["marhenke.blog-s.de/"],
    formats: ["image/webp"],
    unoptimized: true,
  },
};

export default nextConfig;
